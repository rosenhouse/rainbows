# rainbows

An interactive visualization of how a rainbow forms, zooming smoothly from
planetary scale down to the wavefront, including why the bend depends on
wavelength.

## Status

Design exploration. `drafts/` holds review boards from each round; the
build starts once the direction is settled.
